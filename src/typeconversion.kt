fun main(agrs: Array<String>){
    println("Type Conversion")

    var score:Number = 10
    var marks:Long = 20

    //marks = score // Type mismatch will happen
    marks = score.toLong()

    //  +  - /  * % < > >= <= == != +=  -= *= /= %=  && || !
}